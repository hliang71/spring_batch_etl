-- Autogenerated: do not edit this file


CREATE TABLE IF NOT EXISTS BATCH_STAGING_SEQ (ID BIGINT NOT NULL) ENGINE=MYISAM;
Insert Into `BATCH_STAGING_SEQ` (`ID`) 
(Select 0 
From `BATCH_STAGING_SEQ` 
Having Count(*) = 0);

CREATE TABLE IF NOT EXISTS BATCH_STAGING  (
	ID BIGINT  NOT NULL PRIMARY KEY ,  
	VERSION BIGINT NOT NULL,
	JOB_ID BIGINT NOT NULL,
	VALUE LongText NOT NULL,
	PROCESSED CHAR(1) NOT NULL,
	DATE datetime NOT NULL,
	BATCH_KEY VARCHAR(255) NULL ,
	UNIQUE BATCH_KEY_IDX(`BATCH_KEY`)
) ENGINE=MYISAM;

